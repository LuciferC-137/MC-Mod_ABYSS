package wardentools.entity.thryssaryn.individual.behavior.goap;

/**
 The GOAP Planner is a class that implements a Goal-Oriented Action Planning algorithm.
 The creature has a "WorldState" that holds its different states and perceptions.
 When needing to execute a goal, the planner will look for actions that can achieve
 that goal based on the presumed result those actions have on the WorldState.
 */
public class GoapPlanner {
}
